import React from 'react'

export default function Primeiro() {
    const msg = 'E ae cambada!'

    return (
        <div>
            <h1>Primeiro Componente</h1>
            <p>{ msg }</p>
        </div>
    );
}